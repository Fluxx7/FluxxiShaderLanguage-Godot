This is an informal document to try and write out the grammar rules to some degree.

A valid FSL file is composed of any amount of GlobalDeclarations
A valid GlobalDeclaration is either a kernel declaration, a resource declaration, a function declaration, or a constant variable declaration.
A valid function declaration is of the format below:
`type` `function name`(`arg type 1` `arg name 1`, ...) `function body`
