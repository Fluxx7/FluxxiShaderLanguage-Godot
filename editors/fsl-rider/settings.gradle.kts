plugins {
    // Auto-provisions the JDK required by the Java toolchain if it isn't installed.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "fsl-rider"
