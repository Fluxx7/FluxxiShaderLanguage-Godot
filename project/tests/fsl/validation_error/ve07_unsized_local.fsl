//! EXPECT: validation_error
// Unsized arrays are only legal as the last buffer member, not as locals.

layout(std430) buffer sinkBuf {
    float[] sink;
};

kernel[1, 1, 1] unsizedLocal() {
    float[] xs;
    xs[0] = 1.0;
    sink[0] = xs[0];
}
