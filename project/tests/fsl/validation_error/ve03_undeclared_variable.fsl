//! EXPECT: validation_error
// Reads a variable that was never declared anywhere.

layout(std430) buffer sinkBuf {
    f32[] sink;
};

kernel[1, 1, 1] undeclaredVar() {
    sink[0] = never_declared_anywhere;
}
