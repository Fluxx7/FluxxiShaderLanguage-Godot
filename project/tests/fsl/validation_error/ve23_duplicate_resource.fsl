//! EXPECT: validation_error
// Two resources declared with the same block name.

layout(std430) buffer clashing {
    f32[] a;
};

layout(std430) buffer clashing {
    u32[] b;
};

kernel[1, 1, 1] duplicateResource() {
    a[0] = 1.0;
}
