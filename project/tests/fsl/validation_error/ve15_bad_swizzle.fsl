//! EXPECT: validation_error
// Swizzle component out of range for the vector width.

layout(std430) buffer sinkBuf {
    f32[] sink;
};

kernel[1, 1, 1] badSwizzle() {
    f32x2 v = f32x2(1.0, 2.0);
    sink[0] = v.z;
}
