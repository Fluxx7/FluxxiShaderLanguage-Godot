//! EXPECT: validation_error
// float3 initialized from a float2 constructor.

layout(std430) buffer sinkBuf {
    float[] sink;
};

kernel[1, 1, 1] assignMismatch() {
    float3 v = float2(1.0, 2.0);
    sink[0] = v.x;
}
