//! EXPECT: validation_error
// f32x3 initialized from a f32x2 constructor.

layout(std430) buffer sinkBuf {
    f32[] sink;
};

kernel[1, 1, 1] assignMismatch() {
    f32x3 v = f32x2(1.0, 2.0);
    sink[0] = v.x;
}
