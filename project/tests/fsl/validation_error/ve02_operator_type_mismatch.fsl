//! EXPECT: validation_error
// Adding a f32x2 to a f32x3.

layout(std430) buffer sinkBuf {
    f32[] sink;
};

kernel[1, 1, 1] operatorMismatch() {
    f32x2 a = f32x2(1.0, 2.0);
    f32x3 b = f32x3(1.0, 2.0, 3.0);
    f32x3 c = a + b;
    sink[0] = c.x;
}
