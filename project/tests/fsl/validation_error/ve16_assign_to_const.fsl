//! EXPECT: validation_error
// Assignment to a const variable.

layout(std430) buffer sinkBuf {
    f32[] sink;
};

kernel[1, 1, 1] assignToConst() {
    const f32 c = 1.0;
    c = 2.0;
    sink[0] = c;
}
