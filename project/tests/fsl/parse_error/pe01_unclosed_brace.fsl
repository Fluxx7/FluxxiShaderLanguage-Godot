//! EXPECT: parse_error
// Kernel body brace is never closed; file ends mid-scope.

layout(std430) buffer peBuf {
    f32[] xs;
};

kernel[8, 1, 1] broken(g : GlobalInvocationID) {
    xs[g.x] = 1.0;
